[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "cd server && npm install",
  "cd admin && npm install"
]

[phases.build]
cmds = [
  "cd admin && npm run build",
  "cd server && npx prisma generate"
]

[start]
cmd = "cd server && npx prisma db push && node src/index.js"
